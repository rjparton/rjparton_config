set -g prefix C-a

## Theme settings: C-a I to install
set -g @plugin 'egel/tmux-gruvbox'
set -g @tmux-gruvbox 'dark' # or 'light'

set -g status-right '#(gitmux "#{pane_current_path}")'

run '~/.tmux/plugins/tpm/tpm'
